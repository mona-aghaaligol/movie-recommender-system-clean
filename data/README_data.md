# 📁 Data Documentation

This folder contains all datasets used in the **Movie Recommender System** project.

---

## 🎬 Files Overview

| File | Description | Format | Source |
|------|--------------|--------|--------|
| `movies.csv` | List of all movies with their `movieId`, `title`, and `genres`. | CSV | Imported from `movies.json` |
| `ratings.csv` | User ratings for each movie (`userId`, `movieId`, `rating`, `timestamp`). | CSV | Extracted from MongoDB / `ratings.json` |
| `movies.json` | JSON version of the movies dataset used for MongoDB import. | JSON | Converted from `movies.csv` |
| `ratings.json` | JSON version of ratings dataset used for MongoDB import. | JSON | Converted from `ratings.csv` |
| `user_movie_matrix.csv` | User–Movie pivot table used for Collaborative Filtering. | CSV | Generated by `prepare_matrix.py` |
| `similarity_matrix.csv` | Cosine similarity matrix between movies. | CSV | Generated by `compute_similarity.py` |
| `users.csv` | List of users and their names (used for readability in tests). | CSV | Manually created for demo/testing |

---

## 🧠 Data Flow

```mermaid
graph TD
  A[movies.csv] --> B[movies.json]
  C[ratings.csv] --> D[ratings.json]
  D --> E[MongoDB Atlas]
  E --> F[prepare_matrix.py]
  F --> G[user_movie_matrix.csv]
  G --> H[compute_similarity.py]
  H --> I[similarity_matrix.csv]
